services:
  - type: web
    name: cropguard-ai
    env: python
    buildCommand: |
      pip install -r requirements.txt
      cd frontend
      npm install
      npm run build
    startCommand: uvicorn api.index:app --host 0.0.0.0 --port 10000
    plan: free
